mixin headerNav(items)
    nav.header__nav
        ul.header__nav-list
            each item in items
                li.header__nav-item(class=item.nested ? 'header__nav-item--arrow' : '')
                    a.header__nav-link(class=item.current ? 'header__nav-link--current' : '', href="")=item.text